export interface CameraLockCanvasSettings {
	zoom: boolean;
	createFile: boolean;
	select: boolean;
}
export const DEFAULT_SETTINGS: CameraLockCanvasSettings = {
	zoom: true,
	createFile: true,
	select: true,
};
